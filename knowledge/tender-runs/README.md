# Tender-Scan Läufe

Wöchentliche Ergebnisse des Skills `tender-scan`. JSON = Merge aus TED + BKMS + bund.de-RSS (review/maybe), Markdown = gefilterte Entscheidung.

Keine Portal-Passwörter, keine AVPQ-Zugangscodes in diese Ordner schreiben.
